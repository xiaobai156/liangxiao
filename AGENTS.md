# 杀两肖-V2 项目专属规则

本文件适用于 `杀两肖-V2` 及其子目录。处理本项目任务时读取并遵守上级 `..\AGENTS.md`；它是本项目明确继承的统一规则，不依赖工具是否自动加载仓库外父目录。本文件只补充项目差异，未写事项及冲突按上级规则处理，不放宽上级授权和安全边界。

## 工作方式与授权范围

1. 解释、审计或诊断只授权只读检查和提出方案，不自动授权修复。实施仍须满足上级“开始”门槛；用户已明确开始且范围清楚时，连续完成该范围内的编辑和适度验证，不在每一步重复请求确认，也不以只交计划代替实施。
2. 继续同一任务时沿用已确认的范围，不扩大到其他项目、站点、期数或文件。仅在缺少会实质影响结果的信息、下一步超出授权，或触及上级要求单独授权的操作时询问；失败修复的“开始正式更新”仍是独立门槛。
3. 开发与修复遵循 `ponytail full`：先理解受影响代码和真实链路，优先简单实现、复用已有代码，不做无明确收益的抽象或过度设计。不得以极简为由省略安全检查、数据校验、错误处理或必要测试，也不得以技能要求为由缩减用户明确要求的交付。
4. 回复表达遵循 `i-have-adhd` 的已明确偏好：先结论、使用编号步骤、明确下一步；完成时准确说明完成范围，无须用户操作时明确无需额外操作。表达偏好不改变业务验收标准。
5. 仅加载当前任务适用或用户点名的 Skill，使用前完整读取其 `SKILL.md`；不可用时如实说明，继续遵循本文件已明确的要求，不臆造技能内容。不因技能不可用而停止仍可安全完成的工作。
6. 本文件不要求所有任务使用多模型、子 agent、完整开发流程或全量测试。按任务和风险选择必要步骤，保留用户明确指定的外部模型路由；技能不能扩大授权或代替项目验收。
7. 保留现有未提交改动；仅修改本次授权文件，不恢复历史版本或顺带清理其他改动。纯规则文档任务只检查规则、引用和差异，不据此抓取业务站点、读取业务缓存或改动代码、配置、正式输出。

## 项目边界与正式文件

- 生产项目路径：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀两肖-V2`。
- 正式站点配置：`config\sites.json`。
- 正式近10期缓存：`recent_10_cache.json`。
- 兼容生产入口：`two_zodiac_site_scraper.py`，只负责调用 `cli.py`。
- 单期BAT：`运行_手动输入期数.bat`，执行 `two_zodiac_site_scraper.py --period N --workers 10`。
- 多期BAT：`运行_手动输入多期.bat`，执行 `two_zodiac_site_scraper.py --periods N1 N2 ... --workers 10`。
- 单期成功目录：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳`。
- 单期失败目录：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳失败`。
- 成功文件：`{期数}期-二肖.txt`。
- 失败文件：`{期数}期-二肖-失败.txt`。
- 多期全失败汇总：`{期数1}_{期数2}_...期-二肖-多期全部失败.txt`，写入失败目录。

## 杀两肖数据契约

- 每条成功结果必须对应用户手动指定的一个三位期数，期数范围为 `001` 至 `365`。
- 每条结果必须恰好包含两个不同生肖，合法值仅限：`牛马羊鸡狗猪鼠虎兔龙蛇猴`。
- 允许清理两个生肖之间的空格、横线、顿号、逗号和句点，但必须保留网页中的生肖先后顺序；禁止排序或转成无序集合后输出。
- 零个、一个、三个及以上生肖、重复生肖（如“牛牛”）、非法字符或无法证明唯一性的结果必须失败。
- 期数、杀/禁/绝杀/稳杀等二肖资料语义、两个生肖和开奖字段必须来自同一条记录或同一明确资料块。
- 页面标题、作者名、导航、广告、开奖结果、尾数、号码、其他生肖栏目和相邻期不得参与补值。
- `Record.position` 是目标区块内解析出的原始有效候选位置，必须为非负整数；不能在输出或缓存前重新排序、重编号来消除冲突。

## 配置与站点身份

- `config\sites.json` 根节点必须是数组；每项至少具有非空 `name`、合法HTTP/HTTPS `url`、合法 `pick` 和非空 `payload`。
- 配置方向只接受 `top`、`顶部`、`上`、`bottom`、`尾部`、`下`，加载后分别规范化为 `top` 和 `bottom`。
- 当前加载器以“站名 + URL + 方向”拒绝重复身份；新增时还必须额外检查规范化topic、文章ID、用户ID、栏目、字段和目标区块，不能利用加载器较窄的身份键绕过判重。
- 允许的现行来源策略仅为：`page`、`scripts`、`page_and_scripts`、`curl_tls10_page_and_scripts`、`browser_rendered_page`、`admin_article_api`、`tuku_user_forums`、`topic_list_detail`。
- 来源策略名称不是额外抓取授权。脚本、iframe、浏览器等只在该站明确授权的配置范围内使用；各来源独立取证，不跨文档拼接锚点、期数或二肖。允许的来源切换不等于允许解析失败后回退通用解析。
- `parser` 必须绑定 `parsers\registry.py` 中已登记的解析器。已有配置省略 `parser` 时属于兼容的 `named_block`；新增或修复站点必须显式绑定经测试的站点级解析器，不能仅靠 `generic_two_zodiac` 入库。
- `title`、`record`、`stop`、`keywords`、`api_url` 只归对应站点所有；不得把一个站点的正则、锚点、停止边界或接口地址复用于其他站点。
- 配置顺序就是正式输出的站点顺序；新增、修复和缓存同步不得改变无关站点顺序。

## 方向窗口与冲突

在上级规定的候选收集、校验、窗口选择和冲突检查顺序上，使用以下项目约束：

- 无效候选保留诊断，但不得占用方向窗口。
- `top` 只允许目标区块前3条高可信候选；`bottom` 只允许目标区块后3条高可信候选。窗口固定为3条，不得扩大、换向或扫描全页补成功。
- 指定期数不在对应方向窗口内必须失败，即使页面其他位置存在该期。
- 同一方向窗口内，同一期出现不同生肖、不同原始位置或不同权威文档结果，均视为冲突并失败；不能取第一条、最后一条或记录最多的文档消除真实冲突。
- 主DOM、脚本、iframe、解码文本、API、浏览器页面和列表详情分别建立位置序列；文档抓取顺序不得解释为网页顶部或尾部。

## 专属解析与明确特例

- `named_block` 必须使用配置的 `title -> stop` 唯一区块和 `record` 格式；标题缺失、边界不明或同名多块无法唯一定位时失败。
- `site_scoped_two_zodiac` 必须锁定站名/标题及明确二肖语义；不得把整页任意“杀二肖”当作该站结果。
- `parsers\forum.py`、`parsers\dynamic.py`、`parsers\chart.py` 和 `parsers\special.py` 中的解析器均为已登记专属规则，只能服务注册表绑定的站点身份；专属解析失败后不得回退通用解析。
- 图表型资料必须锁定准确列名或表头，例如“禁二肖图”“杀二肖图”“玄机网禁两肖”“澳门杀两肖图”“澳门图库禁肖”；不同图表列不得互相替代。
- “老大主”和“碎银几两”的十肖反推只允许各自专属解析器：原文必须恰好有10个不同合法生肖，再按项目固定生肖序列取缺失的两个生肖；9个、11个、重复或混入其他栏目时失败。
- 用户论坛来源必须按URL中的用户ID锁定接口对象，目标帖子、主题、期数和正文必须属于该用户的同一记录。
- “烟雨扶苏”列表详情策略必须先在列表方向窗口中锁定指定期和专属标题，再读取该期详情脚本；其他期详情或其他标题不得补成功。

## 动态文章与多文档来源

- `admin_article_api` 适用于URL含 `/article/admin/` 或 `/article/manager/` 的已配置动态文章；必须从URL提取文章ID，并唯一匹配同ID接口对象。
- 接口对象的标题、正文、期数、二肖和记录ID必须属于同一个对象；同ID重复、ID缺失、ID不一致、正文解码失败或非可信JSON必须失败。
- 只有专属API返回HTTP 404，且该站已授权对应文章页面/浏览器来源时，才允许进入该路径；网络错误、TLS错误、HTTP 500和接口记录冲突不得兜底。
- 页面或浏览器结果仍必须证明路由只对应原文章ID，并通过站点锚点、指定期、方向窗口、二肖数量和冲突校验。
- `page_and_scripts` 只能收集当前站点允许的页面、同记录脚本、受控iframe和明确引用的嵌套详情；topic/view/read/article记录身份不一致的嵌套文档必须排除或失败。
- 同一权威来源存在不同同期结果时失败。跨文档结果完全一致也必须保留来源证据；不同结果不能以来源顺序、文档长度或解析记录数择一。

## 新增站点

以下流程仅适用于已授权的新增站点任务，不用于普通说明、文档整理或与新增无关的修复：

1. 只读加载 `config\sites.json`、`recent_10_cache.json` 和解析注册表，先检查名称、URL、topic/文章ID、用户ID、栏目、方向和身份冲突。
2. 真实访问用户给出的URL，确认权威来源、站名/作者/栏目锚点、二肖资料语义、区块边界、期数顺序和用户指定方向。
3. 在隔离配置中建立站点级解析器和来源策略，不得先写正式配置或缓存。
4. 新增方向验证的基准期只能选正式缓存最新期或上一期，并明确本次使用的期数；不得从候选网页自行推断。该缓存读取仅用于新增准入，不参与实时单期成功/失败裁决。
5. 正常准入必须取得10个具体期号的有效历史；每期均要保留两个生肖、原始位置和来源证据，动态文章还要保留文章ID。
6. 使用项目正式缓存口径按相同期号比较“完整二肖字符串 + 原始位置”，执行正式判重。
7. 覆盖目标期、相邻期、不存在期、方向越界、错栏目、重复生肖、同期冲突、多文档冲突和缓存冲突测试。
8. 只有全部门禁通过且获得正式写入授权后，才原子写入 `config\sites.json`；随后仅对已授权站点和期数按正式单期链路复抓并核对成功TXT、失败TXT和缓存，遵守下述局部更新边界，不得为验收改跑未授权的全站入口。

- 当前CLI没有已公开的“新增站点并自动写配置”命令，不能把临时脚本、局部解析成功或 `--duplicate-check` 冒充完整新增流程。
- 用户截图只用于确定站名、栏目、期数格式、二肖字段、方向和区块边界，并落实为专属解析测试；截图、粘贴文本和历史输出不能代替本次真实页面或API取证。
- 截图结构与实时来源不一致，或实时来源无法证明截图中的目标区块时必须失败并报告差异，不能把截图里的生肖写死为抓取结果。
- 历史不足10期只能使用上级规则中的单站、单次明确授权；该特例不放宽基准期、方向、来源、栏目、数量、位置或冲突规则。

## 缓存与正式判重

- `recent_10_cache.json` 固定包含 `schema`、`updated_at`、`window_size=10`、连续倒序 `issues` 和 `sites`。
- 每个站点缓存身份至少包含 `name`、`url`、`pick`；每期必须同时保存 `values` 中的二肖和 `positions` 中的原始位置，并保留兼容 `records`。动态文章另保存每期 `article_ids`。
- 单期正式抓取不读取或比较 `recent_10_cache.json` 来判定成功、失败或缓存冲突；本次结果与缓存中的二肖、原始位置或动态文章ID不同，不得因此降为失败。缓存的业务比对只用于新增站点判重及现有缓存判重审计；定稿后的存储维护不属于结果裁决。
- 正常单期成功后仍可维护 `recent_10_cache.json`，供新增站点重复检测；缓存维护不参与本次单期结果裁决。跳期导致未更新时必须明确报告，不能声称已同步。
- `--history-cache --period N` 会重建/覆盖正式近10期窗口，只能在用户明确授权重建缓存后使用；失败站不能用旧值冒充本期成功。
- `--periods` 多期模式逐期生成成功/失败TXT和全失败汇总，但永远不更新 `recent_10_cache.json`。
- `--limit` 仅为单期只读诊断，不写正式TXT、不更新缓存；不得将其结果当正式验收。
- `--duplicate-check` 只审计现有正式缓存覆盖率和重复关系，不抓候选新站，也不接收期数参数。
- 判重只比较双方共同且连续的具体期号，并要求标准化后二肖字符串和原始位置同时相同：连续1至2期不判重复，连续3至5期标记疑似重复，连续6期及以上拒收。
- 缓存缺站、少于10个有效期、缺原始位置、期数窗口不连续、身份重复、JSON损坏或无共同有效期时，判重未完成，不得输出“不重复”。上级允许的历史不足或疑似重复特例必须记录被豁免的门禁与用户授权，不得冒称判重全部通过；连续6期及以上重复仍禁止加入。

## 单期、多期与输出提交

- 期数按数据契约由用户手动指定；脚本不得自动推断当期。
- 单期正式流程按配置顺序输出 `二肖 站名`，可选 `--include-url` 才附加URL；末尾排行榜固定使用制表符分隔的 `内容、次数、排名` 三列，次数不带“次”字，相同次数使用相同连续排名（如 `1、2、2、3`）。
- 正式成功TXT不得追加固定尾注：`黄杀`、`有点帅`、`金绝`、`金元宝`、`男人牛`；新生成的完整输出只保留站点结果和排行榜。历史文件不自动清理；局部追加遵守下节逐字节保留要求，不重算原有排行榜。
- 成功去重只允许完全相同的“二肖字符串 + 站名”重复展示；不同站名不能因二肖相同而丢弃。
- 正式写入使用锁、同目录临时文件和原子替换。成功TXT与失败TXT构成输出提交单元，提交失败时恢复本次输出改动并报告“输出写入未完成”，不得声称文件已同步；不改变已完成校验的数据判定。
- 缓存维护在本次结果定稿后进行，与输出提交分开报告。缓存写入失败只回滚本次未完成的缓存改动，报告“缓存更新未完成”；不得撤销已提交的有效输出或将已定稿成功结果改判为抓取失败。
- 全部站点成功且本次删除授权包含该期失败TXT时，才删除该文件；否则保留并报告待清理。仍有失败时，失败报告沿用上级要求的完整字段，不能只写站名和笼统原因。局部修复不得套用全期失败文件删除规则。
- 多期模式每期独立生成 `{期数}期-二肖.txt` 和失败TXT；某站任一期成功不影响其他期结果，只有所有输入期均失败的站点进入多期全失败汇总。
- 限量诊断、独立失败验证和测试不得写正式TXT或正式缓存。

## 失败站点修复与局部正式更新

本节适用于点名站点的新增落地或失败修复，不用于改写普通全量单期流程。失败修复先独立验证，用户再次明确“开始正式更新”后才迁移已验证的最小改动；独立验证不能提前执行下述正式写入。

- 新增或修复的真实抓取只运行用户本次明确指定的站点，禁止运行全站BAT、全站CLI或抓取任何未指定站点。隔离离线测试按变更影响范围选择；需要项目完整测试时，也不得夹带未授权的网络抓取或正式文件写入。
- 独立验证只使用隔离目录，并核对正式文件前后哈希。确认当前实际存在且支持指定范围的入口；没有受限正式入口时报告“待正式入口验收”，不得虚构命令或改用全站入口。修复不扩大三条窗口、不放宽专属语义和来源身份校验。
- 不得抓取、改写、重算、重排或以任何方式影响其他站点的数据、配置、输出和缓存。
- 指定站点成功且该站本期结果尚未写入时，成功TXT只追加该站点本期结果；追加位置固定在原有站点结果区底部、历史固定尾注（如有）和排行榜表头 `内容、次数、排名` 之前，禁止写到排行榜内容之后。逐字节保留已有内容、值和顺序，不重算排行榜或清理历史尾注；相同结果已存在则不重复追加，已有不同结果则停止该项写入并报告冲突，不自行覆盖或并列追加。
- 局部追加后报告“原排行榜未重算”，不得宣称排行已反映新增结果。若用户需要更新排行或替换已有不同结果，先取得对应内容的修改授权，不自行放宽逐字节保留要求。
- 指定站点失败时，只新增或更新该站点本期失败记录；不得新增、改写或删除其他站点的失败记录。
- 局部单期缓存只更新指定站点的本期结果；其他站点缓存以及指定站点的其他期数必须保持不变，禁止借机滚动窗口或重建整份缓存。允许为原子提交替换JSON文件，但不允许借物理替换改变无关内容；若当前窗口无法容纳本期而不改变上述内容，则报告未同步并另行取得窗口更新授权。多期模式仍不更新正式缓存。
- 局部更新沿用上一节输出与缓存分开提交的失败边界；输出提交失败只回滚本次指定站点的输出改动，缓存提交失败不回滚已成功提交的输出。不得回滚用户已有改动。

## 按范围验收与报告

1. 纯文档任务检查继承关系、冲突、独有约束保留情况、路径/命令引用和本次差异；已有Git改动须与本轮新增改动区分，不要求为文档整理运行爬虫、Python编译或全量测试。可报告“文档修改及文本检查完成”，同时说明“爬虫业务未验证”，不得据此声称新增或修复完成。
2. 代码、配置、解析器和输出变更按上级验收规则选择受影响测试、编译/静态检查及真实指定期流程；共享流程、统一校验、缓存、存储或入口变更再运行项目完整测试。真实抓取与正式文件写入仍以已授权站点和范围为限。
3. 新增和修复应证明指定期、两个生肖、原始顺序/位置、方向窗口、栏目语义、文章或用户身份和独立来源，并覆盖上级要求的边界与冲突。缓存冲突测试须区分新增判重门禁和实时单期不受旧缓存裁决的规则。
4. 按本次适用环节报告代码/文档修改、离线检查、真实验证、正式入口验收、输出提交和缓存维护的状态，不要求每次列出无关环节；未执行的必要检查说明缺口。缺少真实验证或正式入口验收时，业务只报告部分完成或待验证，不把解析成功、离线测试通过或规则写入冒充业务完成。

## GitHub 同步

- 本项目绑定的 Git 远程为 `origin`：`https://github.com/xiaobai156/liangxiao.git`；只同步当前项目目录，不操作其他项目或其他远程。
- 从本规则生效后，当前项目内已获授权的修改完成必要验证且检查通过时，自动检查差异、提交并推送到当前分支的 `origin`；提交信息应说明本次实际改动。
- 自动提交只包含本次任务产生且属于当前项目的源码、配置、测试和文档。已有未提交改动必须保留；若同一文件混有历史改动和本次改动，能安全分离时只暂存本次相关区块，不能安全分离时不提交该文件并报告原因。
- 不使用 `git add -A` 盲目打包；提交前检查文件清单和内容，排除凭据、密钥、令牌、`.env`、临时文件、日志、`__pycache__`、`*.pyc`、覆盖率文件、锁文件、业务缓存、缓存备份、隔离证据和无关路径。
- 推送前至少完成与本次变更范围匹配的测试或静态检查、`git diff --check` 和敏感文件检查；验证失败不得提交或推送。网络、权限、远程分支冲突或提交范围无法安全确认时，停止推送并如实报告，不强推、不改写远程历史。
- 本规则是每次本项目任务结束时的提交/推送约束，不建立后台文件监视器；未获授权的解释、审计、诊断和独立验证仍不得因此自动提交或推送。
