# 杀两肖-V2 项目规则

本文件继承父目录 `C:\Users\Administrator\Desktop\每天工具\爬虫合集\AGENTS.md`，只补充本项目差异；未写事项按父级执行，冲突时以父级为准。本文件不放宽授权、安全、来源隔离或数据正确性要求。

## 1. 工作方式

- 开发和修复遵循 `ponytail full`：先阅读受影响代码和真实链路，优先复用和最小修改；不因简化省略校验、错误处理或必要测试。
- 回复先给结论，再用编号说明结果和下一步。当前未发现可读取的 `i-have-adhd` Skill，按用户明确的表达偏好执行。
- 解释、审计、诊断不自动授权修改；用户已明确“开始”且范围清楚时直接完成，不重复确认。保留已有未提交改动；不顺带清理其他项目。

## 2. 项目文件与入口

- 项目目录：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀两肖-V2`。
- 配置：`config\sites.json`；正式缓存：`recent_10_cache.json`。
- 主入口：`cli.py`；兼容入口：`two_zodiac_site_scraper.py`；单期/多期 BAT 仅用于全站流程。
- 成功目录：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳`；失败目录：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳失败`。
- 文件格式：`{期数}期-二肖.txt`、`{期数}期-二肖-失败.txt`。
- 新增、修复和失败重抓不得调用全站 BAT 或全站入口。失败重抓命令为 `python cli.py --period <期数> --retry-failures`，只读取该期失败 TXT 中按站点名、方向和 URL 完整匹配的站点。

## 3. 杀两肖数据契约

- 每条结果对应用户指定的 `001`–`365` 期，恰好包含两个不同生肖；合法顺序为 `牛马羊鸡狗猪鼠虎兔龙蛇猴`。
- 可清理生肖之间的空格、横线、顿号、逗号和句点，但保留网页原始生肖顺序，不能排序或转集合。
- `Record.position` 是目标区块的原始有效位置，不得重编号消除冲突。
- `top`/`bottom` 只取目标区块有效候选的前3条/后3条；窗口固定，不得扩大、换向或跨区块补值。
- 期数、二肖语义、两个生肖、位置和开奖字段必须来自同一条记录或明确资料块；无法证明唯一性即失败。
- 成功 TXT 行为 `二肖 站名`，可选附加 URL；排行榜为 `内容\t次数\t排名`，次数不带“次”，相同次数使用相同连续排名。
- 正式成功 TXT 不自动追加 `黄杀`、`有点帅`、`金绝`、`金元宝`、`男人牛`；历史文件不自动清理，局部追加不重算排行榜。

## 4. 解析差异

- 站点必须使用 `parsers\registry.py` 已登记的显式 parser；专属 parser 失败不得回退通用 parser。
- `named_block` 必须唯一锁定配置的 `title -> stop` 区块；`site_scoped_two_zodiac` 必须同时锁定站点锚点和二肖语义。
- `parsers\forum.py` 按 URL 用户 ID 锁定用户对象；`parsers\dynamic.py` 按文章 ID 锁定唯一接口对象；列表详情先锁定方向窗口中的期数和标题，再读取详情。
- 图表解析必须锁定配置表头/列名；“老大主”和“碎银几两”的十肖反推只能使用各自专属 parser，原文必须恰好有10个不同合法生肖。
- 页面、脚本、iframe、API、浏览器和列表详情仍按父级规则作为独立来源，不得跨来源拼接结果。

## 5. 新增、修复与局部重抓

- 用户明确项目、站点、URL、期数、方向和字段后，只处理该范围；不抓取、不改写、不影响其他站点。
- 新增先检查名称、URL/文章 ID/用户 ID、栏目、方向和身份，再做真实来源、专属解析、历史和判重；历史不足10期或连续3–5期疑似重复，只有用户点名授权才放行，连续6期禁止加入。
- 修复先独立验证；用户明确“开始正式更新”后才写正式配置、TXT或缓存。
- 失败 TXT 重抓成功后，只追加该站点成功行、清除对应失败行并更新该站点当期缓存；仍失败保留原失败记录。其他站点、其他期数和无关文件不变。
- 失败重抓的成功 TXT、失败 TXT、缓存路径必须互不重合；输出使用公共输出锁和原子替换。输出提交失败只回滚本轮输出，缓存提交失败保留有效输出并报告“缓存未同步”。
- 成功 TXT 按“生肖＋站名”去重；已有同站不同生肖时停止该站写入并报告冲突。`--include-url` 的变化不能造成重复。
- 局部缓存只能更新目标站点的目标期；先执行完整缓存结构校验，期数不在现有窗口时保留缓存并报告未同步。配置指纹不作为缓存与当前配置的绑定条件。

## 6. 验收与同步

- 代码变更运行受影响测试、编译/静态检查和真实指定期流程；只有修改共享流程、缓存、存储或入口时才追加项目完整测试。禁止为测试跑未授权全站抓取。
- 未完成真实验证只能报告待验证；成功 TXT、失败 TXT 和缓存需分别核对目标站点范围及无关内容未变。
- 本项目 `origin` 为 `https://github.com/xiaobai156/liangxiao.git`。已授权修改完成必要验证后，提交并推送本次相关源码、测试、配置或文档；排除业务缓存、诊断证据、备份、日志、覆盖率、锁文件、`__pycache__`、`*.pyc` 和无关既有改动。
